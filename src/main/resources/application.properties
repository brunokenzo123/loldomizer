spring.application.name=loldomizer
# Configurações de Conexão com o MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/loldomizer?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Bki220103#

# Permite que o Spring crie as tabelas no banco de dados automaticamente
spring.jpa.hibernate.ddl-auto=update

# Mostra no console os comandos SQL que o Java está executando por baixo dos panos
spring.jpa.show-sql=true